# Clikt 参数体系交底：argv 切分 → value 落定 → validate 调用

本文所有行号对应当前 checkout。约定：路径一律写成 `模块/相对路径/文件.kt:行号`。本文只涉及 `clikt/` 模块的 commonMain 源码；配套钉结论的测试在 `test/src/commonTest/kotlin/com/github/ajalt/clikt/parsers/FinalizationOrderTest.kt`。

## 1. 调用链：从 `CoreCliktCommand.parse(argv)` 到两个终点

以一个**普通（非 eager、不属于任何组）选项** `val o by option()` 为追踪对象。整条链分两段：先把 argv 变成 `CommandInvocation`（不抛异常，错误挂在 invocation 上），再在 `run` 时 flatten 并逐命令终局化。

### 1.1 解析阶段：argv → invocation（不抛异常）

1. `CoreCliktCommand.parse(argv: List<String>)` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/CoreCliktCommand.kt:106`（实现在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/CoreCliktCommand.kt:107`）。公开入口，直接委托给 `CommandLineParser.parseAndRun(this, argv) { it.run() }`：先解析、再 flatten 终局化、最后逐命令回调 `run()`。
2. `CommandLineParser.parseAndRun` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:70`。第 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:75` 行调 `parse(command, argv)` 得到 `CommandLineParseResult`（只解析、不终局化、不抛），第 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:76` 行调 `run(result.invocation, runCommand)` 进入终局化。
3. `CommandLineParser.parse` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:105`，体在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:106` 调内部的 `parseArgv`。
4. `parseArgv` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:8`。`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:12` 先 `rootCommand.resetContext()`（每次 parse 重建 Context 树、把选项 delegate 复位为未赋值），`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:13` 调 `parseCommandArgv(rootCommand, originalArgv, 0)`，`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:14` 包成 `CommandLineParseResult`。
5. `parseCommandArgv` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:18`。`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:23` 构造 `CommandParser(command, argv, i).parse()` 解析当前这一层；`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:28`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:34` 是 while 循环：只要本层解析出了下一个子命令（且它的父 Context 正是当前命令），就用同一段 expanded argv 和停下位置 `i` 递归解析子命令，子调用结果挂进 `subcommands`。这是子命令链（含 `allowMultipleSubcommands` 链式下钻）在解析期的展开点。
6. `CommandParser.parse` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:87`。四步：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:88` `splitOptionPrefixes()`（把每个 Option 的名字建成 `optionsByName`/`longNames`/`prefixes` 查找表，并对名字不变量做 `require`）；`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:89` `printHelpOnEmptyArgsIfNecessary()`（空 argv 且要求空参打印帮助时，往 `errors` 塞一个 `PrintHelpMessage` 后直接返回）；`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:90` `consumeTokens()`；`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:91` `parseArguments()`；`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:92` `makeResult()`。
7. `consumeTokens` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:95`。逐 token 的 `when` 分派，顺序很重要：
   - 别名：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:101`（在 `minAliasI` 之前的位置不展开，防止别名递归）→ `insertTokens` 原地展开，不推进 `i`；
   - `@文件`：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:106`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:113`，`@@x` 当普通参数，`@x` 经 shlex 展开后插回 token 流；
   - `--`：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:117`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:121`，此后 `canParseOptions=false`；
   - 长选项形态：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:123`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:130` → `parseLongOpt(tok)`；
   - 短选项形态（分组短选项）：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:132`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:137` → `parseShortOpt(tok)`；
   - 子命令：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:139`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:143`（前提 `canParseSubcommands`，即已收集的参数 token 数不少于必填参数个数），记下 `subcommand` 后 `break`——**当前命令在子命令名处立刻停止消费，剩余 token 原样留给子命令**；
   - 其余：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:145`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:150`，进 `argumentTokens`；`allowInterspersedArgs=false` 时同时关闭选项解析。
8. `parseLongOpt`/`parseShortOpt` → `parseOptValues` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:220` / `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:241` / `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:282`。选项名查 `optionsByName`；查不到时，`treatUnknownOptionsAsArgs` 为真就当未知 token 留给参数（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:227`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:229`），否则造一个 `NoSuchOption` 错误（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:231`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:236`）。`parseOptValues` 在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:294`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:305` 向后吞值，遇 `--`、已知选项名、子命令名、或任何带选项前缀的 token（且未开启 `treatUnknownOptionsAsArgs`）就提前停；值不够不在这里报错（注释 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:307`），留给终局化。
9. `consumeOptionParse` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:205`。`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:207` 把每次调用记成 `OptInvocation`，`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:208`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:211` 有 `err` 就入 `errors` **并立刻置 `context.errorEncountered = true`**（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:210`），`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:212` 推进 `i`。
10. `parseArguments` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:312`。按 `_arguments` 顺序把 `argumentTokens` 切成 `ArgumentInvocation`；值不够时在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:332`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:334` 提前 `return`（因此多余参数错误 `handleExcessArgs` 在这种输入下不会走到）；多余 token 走 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:343`：恰好一个且存在子命令时报 `NoSuchSubcommand`，否则 `NoSuchArgument`。
11. `makeResult` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:183`。`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:184` 把调用记录按 `Option` 分组成 `Map<Option, List<OptionInvocation>>`；`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:185`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:188` 给每个错误补 Context 并置 `errorEncountered`；关键在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:189`：`val nextCommand = subcommand.takeIf { errors.isEmpty() }`——**本层一旦有解析错误，已识别出的子命令被丢弃，子树不会被解析**。

### 1.2 终局化阶段：invocation → value 赋值 → validate 调用

12. `CommandLineParser.run` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:86`。`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:90` `rootInvocation.flatten().use { ... }`，`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:91`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:93` 对 flatten 出来的每个 invocation 调 `runCommand`（即 `it.run()`）。
13. `CommandInvocation.flatten` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/Invocation.kt:101`，返回 `FlatInvocations`。树的展开在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/Invocation.kt:109`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/Invocation.kt:119`：前序 DFS，`yield` 一个命令后递归它的 `subcommandInvocations`。真正的终局化挂在迭代器上：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/Invocation.kt:127` 先把整棵树所有命令的 `finalizeEagerOptions` **全部**跑一遍，然后 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/Invocation.kt:128` 才在 `onEach` 中按同一前序逐个 `finalizeCommand`，每 yield 一个就轮到一次 `run()`。两趟分开是故意的（注释 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/Invocation.kt:125`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/Invocation.kt:126`）：保证子命令上的 `--help` 能先于父命令必填项检查生效。
14. `CommandLineParser.finalizeEagerOptions` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:117`。`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:120` 先查 shell 补全环境变量；`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:122`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:123` 用 `getOpts`/`getInvs`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:171`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:178`）按 `Option.eager` 二分。`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:127` 终局化 eager 选项；`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:128` 立即对它们做 `postValidate` 并 `throwErrors()`。注意注释 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:125`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:126`：eager 选项**只有被调用时**才校验。`--help` 本身是 `BaseCliktCommand.resetContext` 时注册的 eager 选项（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/BaseCliktCommand.kt:129`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/BaseCliktCommand.kt:131`），其动作 `throw PrintHelpMessage(context)`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/BaseCliktCommand.kt:130`）在这里同步抛出。eager 选项在实现上是 `.flag().validate { if (it) action() }`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/EagerOption.kt:51`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/EagerOption.kt:57`），所以动作最终也走 `postValidate` 这一跳。
15. `CommandLineParser.finalizeCommand` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:138`。逐跳：
    - `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:141`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:145` 取注册的 group、argument、非 eager 选项及其调用记录；
    - `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:148` `invocation.getUsageErrorsOrThrow()`（定义在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:184`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:189`）：解析阶段的错误全是 `UsageError` 则攒起来；一旦混入非 `UsageError`（当前唯一来源是 `printHelpOnEmptyArgs` 塞的 `PrintHelpMessage`），在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:188` **立刻抛出**，其余 UsageError 随之丢弃；
    - `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:151` 过滤出**不属于任何组**的非 eager 选项；
    - `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:153`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:155` 调 `finalizeParameters` 做真正的赋值，返回终局化错误；
    - `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:157` `(usageErrors + finalizationErrors).throwErrors()`——解析错误与终局化错误**在这一行合流**并抛出（见不变量 3）；
    - `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:159` 只有上一行没抛，才会执行 `validateParameters(...)` 并抛校验错误；
    - `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:161`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:166` 没有子命令却注册了子命令且 `invokeWithoutSubcommand=false` 时，抛 `PrintHelpMessage(context, error = true)`；
    - `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:168` 记录本命令的 invokedSubcommands。
16. `finalizeParameters` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:29`。`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:39`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:46` 先按 `Option.group` 把调用记录分组（`null` 键收纳非组选项），再补入所有未被调用的 group；`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:48` 用 `getAllOptions`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:66`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:73`）把未调用的非组选项以空 invocation 补齐；`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:50`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:55` 同样补齐未调用的 argument；最后 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:57`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:61` 按固定顺序拼出 `allParams`：**先 argument，再非组 option，最后 group**。组内选项不在这个列表里——它们在组被终局化时才递归处理。
17. `iterateFinalization` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:77`。`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:87`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:104` 对本轮每个参数调 `finalize`：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:90` argument、`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:91` option、`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:92` group。异常分派：`LateinitException` 在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:94`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:95` 丢进 `nextRound` 等下一轮重放（跨参数依赖默认值靠它）；`UsageError` 在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:96`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:99` 收集并置 `errorEncountered`；`Abort` 在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:100`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:103` 仅当此前没有任何错误时才上抛。循环退出条件在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:105`：`nextRound` 为空（全部完成）或 `nextRound.size >= currentRound.size`（本轮没有任何进展，再跑也不会收敛）。
18. 组选项的中间一跳：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:92` → `OptionGroup.finalize` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/groups/ParameterGroup.kt:93`，体在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/groups/ParameterGroup.kt:97` 调 `finalizeOptions`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:13`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:21`），对组内选项再跑一遍同一个 `iterateFinalization`。
19. **终点一（value 落定）**：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:91` → `OptionWithValuesImpl.finalize` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/OptionWithValues.kt:190`。`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/OptionWithValues.kt:191` 经 `getFinalValue` 合并 envvar / ValueSource（普通选项的默认值不在这一步，而是 transformAll 里处理），`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/OptionWithValues.kt:194`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/OptionWithValues.kt:196` 仅对命令行调用校验 `nvalues`；**`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/OptionWithValues.kt:200` 这一行 `value = transformAll(...)` 就是普通选项最终值被真正赋值的位置**（`transformEach`/`transformValue` 在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/OptionWithValues.kt:201`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/OptionWithValues.kt:202` 对每次调用的每个字符串值做转换）。argument 的对应位置是 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/arguments/Argument.kt:205`。
20. `validateParameters` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:112`。顺序与终局化不同：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:119`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:121` 先非组 option，`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:122`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:124` 再 group，`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:125`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:127` 最后 argument；每个调用包在 `gatherErrors`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:131`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:143`）里，只捕获 `UsageError`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:138`），收集并置 `errorEncountered`。组的一跳是 `OptionGroup.postValidate` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/groups/ParameterGroup.kt:100`，按组内注册顺序逐个调选项的 `postValidate`。
21. **终点二（`validate { }` 回调被调用）**：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:120` → `OptionWithValuesImpl.postValidate` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/OptionWithValues.kt:215`，体在 **`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/OptionWithValues.kt:216` `transformValidator(OptionTransformContext(this, context), value)`**。`validate { }` 是通过 `copy` 替换 `transformValidator` 挂上去的（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/Validate.kt:23`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/Validate.kt:25`），且包了一层 `if (it != null)`——最终值为 null 时用户的校验块不会执行。argument 的对应位置是 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/arguments/Argument.kt:208`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/arguments/Argument.kt:210`。
22. 错误的实际抛出：`throwErrors` — `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Util.kt:11`，委托给 `MultiUsageError.buildOrNull`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/exceptions.kt:148`）：空列表不抛、单个错误原样抛、多个才包成 `MultiUsageError`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/exceptions.kt:152`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/exceptions.kt:160` 还会在遇到 `NoSuchOption` 后过滤掉后续 `NoSuchArgument`）。
23. 命令体执行：回到 `CommandLineParser.run` 的 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:92`，此刻该命令所有参数的 `value` 都已赋值、`validate{}` 都已跑过，`it.run()` 才被调用。

### 1.3 子命令在命令树上的展开

- 解析期：`CommandParser.consumeTokens` 在子命令名处 `break`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:139`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:143`），把剩余 argv 和停下位置 `i` 交给 `parseCommandArgv` 递归（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:28`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:34`），递归结果在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:33` 挂到父 invocation 的 `subcommandInvocations`。父层有任何解析错误时 `makeResult` 丢弃子命令（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:189`），子树整体不解析。`allowMultipleSubcommands` 时祖先命令的子命令也会被纳入 `allSubcommands`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:56`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:65`），while 循环可继续向兄弟子命令链下钻。
- 终局化期：`FlatInvocations` 前序遍历（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/Invocation.kt:109`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/Invocation.kt:119`），但分成两趟：**第一趟所有命令的 eager 选项**（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/Invocation.kt:127`），**第二趟才按前序逐个 `finalizeCommand` + `run()`**（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/Invocation.kt:128`、`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:91`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:93`）。
- 直接推论：`sub --help` 时，子命令的 `PrintHelpMessage` 在第一趟抛出（注册动作 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/BaseCliktCommand.kt:130`），父命令那个 `required()` 选项的 `MissingOption` 要到第二趟 `finalizeCommand` 才会产生，所以最终抛出的是 `PrintHelpMessage`。该结论由 `FinalizationOrderTest.subcommand_help_wins_over_parent_required_option` 钉死。

## 2. 不变量

### 不变量 1：参数的最终值只有在自身 `finalize` 成功返回后才可读

- 断言：任何参数 delegate 在它自己的 `finalize` 执行完成前被读，必然抛 `LateinitException`，绝不会读到半转换的值或旧值；`finalize` 成功的标志就是给 delegate 赋了值（option 在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/OptionWithValues.kt:200`，argument 在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/arguments/Argument.kt:205`）。
- 保证：delegate 是 `NullableLateinit`，读时哨兵检查在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/internal/NullableLateinit.kt:17`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/internal/NullableLateinit.kt:19`；每次 parse 还会 `resetContext()`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:12`）重建选项对象，上一轮的值不残留。跨参数读取（例如 `defaultLazy { other }`）被允许，但读早了抛的 `LateinitException` 会被终局化循环捕获、把该参数推迟到下一轮重放（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:94`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:95`）。
- 破坏后的外部表现：读到 `"Cannot read from option delegate before parsing command line"` 的 `IllegalStateException`；若形成环形依赖（见风险点 1），该异常在终局化阶段被吞，最终在 `postValidate` 读 `value`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/OptionWithValues.kt:216`）或命令体里读 delegate 时才炸，调用方完全定位不到是哪个选项成环。

### 不变量 2：终局化与校验有固定且不同的两类顺序

- 断言：`transformAll`（即赋值那一刻）的顺序是 **argument → 非组 option → 组**；`postValidate`（即 `validate { }`）的顺序是 **非组 option → 组 → argument**。组内部在两个阶段都按选项注册顺序。
- 保证：赋值顺序由 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:57`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:61` 的 `buildList` 顺序保证；校验顺序由 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:119`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:127` 三个 for 循环保证；组内选项的展开在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/groups/ParameterGroup.kt:97`（终局化）与 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/groups/ParameterGroup.kt:100`（校验）。
- 破坏后的外部表现：调换这两个列表的构造顺序，`defaultLazy` 跨参数读取会改变需要重放的轮数（不影响单跳依赖的最终值，但环形/多重依赖的报错时机与会被调用的 lambda 次数会变），`validate { }` 里的副作用顺序以及 `MultiUsageError` 里错误的排列顺序会静默重排。该顺序由 `FinalizationOrderTest.transformAll_order_is_argument_option_group` 和 `FinalizationOrderTest.postValidate_order_is_option_group_argument` 钉死。

### 不变量 3：哪些错误被攒起来、哪些立刻抛，有明确分界

- 断言：`UsageError` 永远被攒起来批量抛出；非 `UsageError` 立刻抛出并截断后续。具体三处：(a) 解析阶段的错误一律进 `invocation.errors`，`parse` 不抛（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:208`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:211`）；(b) `finalizeCommand` 入口发现错误列表里混入非 `UsageError` 时立刻抛（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:184`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:189`，当前唯一来源是 `printHelpOnEmptyArgs` 的 `PrintHelpMessage`，由 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:154`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:162` 放入）；(c) eager 选项动作抛的非 UsageError（如 `--help`）在第一趟终局化同步抛出（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/Invocation.kt:127` → `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:127`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:128`，动作注册在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/BaseCliktCommand.kt:130`）。
- 保证：聚合只通过 `throwErrors`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Util.kt:11`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Util.kt:13`）→ `MultiUsageError.buildOrNull`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/exceptions.kt:148`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/exceptions.kt:161`），0 个不抛、1 个原样、多个才包 `MultiUsageError`。
- 破坏后的外部表现：若把非 UsageError 也塞进列表（或把 (b) 的立即抛出改成收集），`--help`/补全消息会被 `MultiUsageError` 吞成普通用法错误，`--help` 不再打印帮助；反过来，若解析错误在解析期就抛，`MultiUsageError` 一次回吐多个错误的能力消失（只报第一个，后面的错误用户要多次运行才能看到）。

### 不变量 4：解析错误与终局化错误合流，但校验错误永远是第二批

- 断言：解析阶段攒下的 `UsageError` 和 `finalize`/`transformAll` 抛出的 `UsageError` 合为一次抛出，发生在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:157`；而 `validateParameters` 在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:159`，**只有前一行没抛才会执行**。因此 `validate { }` 的错误从不和转换/缺失类错误出现在同一个 `MultiUsageError` 里。
- 保证：`getUsageErrorsOrThrow`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:184`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:189`）先取解析错误；`finalizeParameters` 返回终局化错误（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:63`）；两者在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:157` 相加后由 `throwErrors` 抛出；`validateParameters`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:112`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:129`）在它之后。
- 破坏后的外部表现：若调整这两行的先后或把两批合并，只要存在一个转换错误，`validate { }` 就可能被跳过或读到未赋值 delegate（`postValidate` 会直接读 `value`，见 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/OptionWithValues.kt:216`），上游做“批量回吐校验错误”的扩展时会在同一个 `MultiUsageError` 里看到或看不到校验类错误，行为随顺序静默改变（实测见风险点 3）。

### 不变量 5：`Context.errorEncountered` 只决定一件事——`Abort` 是吞还是抛

- 断言：该标志一旦置位，同一 Context 后续终局化循环里抛出的 `Abort` 被忽略；否则 `Abort` 立即上抛。它不改变 `UsageError` 的收集，也不阻止后续参数继续 `finalize`。
- 置位者：解析期两处——`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:187`（makeResult 遍历错误时，任何错误都置位）和 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:210`（单个选项解析出错时）；终局化两处——`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:99`（finalize 抛 UsageError）和 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:141`（postValidate 抛 UsageError）。声明与语义在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/Context.kt:143`（文档注释 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/Context.kt:136`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/Context.kt:142`），`internal set` 保证外部不能改。
- 读者：唯一读取点 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:102`。
- 破坏后的外部表现：若置位点被漏掉（例如解析期错误不再置位），用户文档承诺的“想跳过后续转换就抛 `Abort`”（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/Context.kt:140`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/core/Context.kt:142`）会在有解析错误时反而中断终局化，原本可以继续收集的错误（如必填项缺失）不再回吐；若无条件吞 `Abort`，则自定义 transform 里用 `Abort` 做正常短路（例如 `allowMultipleSubcommands` 场景）会永久失效。

### 不变量 6：eager 选项先于整棵树的非 eager 终局化，且只在被调用时校验

- 断言：所有命令（含子命令）的 eager 选项终局化与校验都先于任何命令的非 eager 终局化；未被调用的 eager 选项不做 `postValidate`。
- 保证：两趟 flatten 在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/Invocation.kt:127`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/Invocation.kt:128`；eager 过滤在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:122`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:123`（`getOpts`/`getInvs` 按 `eager` partition，`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:171`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:178`），校验只传 `eagerInvs.keys`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:128`）。
- 破坏后的外部表现：`sub --help` 会被父命令的 `required()` 选项拦住而报 `MissingOption`（实测见测试一：当前抛出的是 `PrintHelpMessage`，`error=false`）。

## 3. 风险点

三个风险分别落在 `internal/`、`parsers/`、`parsers/CommandLineParser` 三处，均为已实测的行为（复现用 `CliktCommand`，它就是 `CoreCliktCommand` 的子类；测试里等价写法用 `TestCommand`，结论由 `FinalizationOrderTest` 相关用例钉住）。

### 风险点 1：终局化循环对环形依赖静默收敛，选项永远停在未赋值状态

- 位置：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:105`（退出条件），配合 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:94`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:95` 对 `LateinitException` 的静默重试。
- 机理：`defaultLazy { }` 的默认值回调在 `transformAll` 里执行（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/TransformAll.kt:91`）。两个 `defaultLazy` 互相引用时，第一轮两者都因读到未赋值 delegate 抛 `LateinitException` 进 `nextRound`；第二轮再次双双失败，此时 `currentRound.size <= nextRound.size`（2 <= 2）成立，循环退出，`errors` 是空的——`finalizeParameters` 正常返回，没有任何报错。之后 `finalizeCommand` 继续走到 `validateParameters`，而 `OptionWithValues.postValidate` 会无条件读 `value`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/OptionWithValues.kt:216`），异常会在一个令人意外的位置——该校验阶段而非 defaultLazy 回调里——炸出来。
- 最小复现：

  ```kotlin
  class C : CliktCommand() {
      val a: String by option().defaultLazy { b }
      val b: String by option().defaultLazy { a }
      override fun run() = Unit
  }
  C().parse(emptyList())
  ```

  argv 为空（两个选项都走默认值）。实际观察：`parse` 抛出 `IllegalStateException`（内部类型 `LateinitException`），消息为字面量 `Cannot read from option delegate before parsing command line`；它不在 `defaultLazy` 回调处抛出、也不是 `UsageError`，上游完全拿不到“哪个选项成环”的信息。若两个选项都没有 `validate`/`postValidate` 读值（例如自定义 `Option` 的 validator 不碰 value），`parse` 甚至会成功，异常延后到 `run()` 里第一次读 delegate 时才炸。
- 最小修法：循环结束后若 `nextRound` 非空，不再静默返回，而是把最后捕获的 `LateinitException`（或包装成一个 `UsageError`）加入 `errors` 抛出，让成环/不可满足依赖在终局化当场报错。
- 钉结论：`FinalizationOrderTest.defaultLazy_mutual_reference_throws_on_read`。

### 风险点 2：父命令一个解析错误会让整棵子命令树被静默丢弃

- 位置：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:189`（`subcommand.takeIf { errors.isEmpty() }`），配合递归入口 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:28`。
- 机理：子命令名在 `consumeTokens` 中被识别后只是记下并 `break`（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:139`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:143`），真正递归解析子命令发生在 `parseCommandArgv` 的 while 条件（`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:28`）里，而条件依赖 `rootResult.nextCommand`。`makeResult` 在父层 `errors` 非空时把它置成 `null`，于是**已经出现在 argv 里的子命令及其后续 token 全部不解析**。这不仅吞掉子命令上的 `--help`，也使子命令本该产生的解析错误（未知选项、缺参数等）一条都不会出现——用户看不到子命令视角的任何错误，只能看到父层第一个错误。
- 最小复现：

  ```kotlin
  class Sub : CliktCommand() { override fun run() = Unit }
  class C : CliktCommand() { override fun run() = Unit }
  C().subcommands(Sub()).parse(listOf("--typo", "sub", "--help"))
  ```

  实际观察：抛出 `NoSuchOption`，消息为 `no such option --typo`；同一个命令在 argv 为 `listOf("sub", "--help")` 时正常抛 `PrintHelpMessage(error=false)`。即父层加一个拼错的选项后，`sub --help` 被完全无视。`allowMultipleSubcommands` 场景下，链上任何一层的一个错误都会截断整链解析。
- 最小修法：不要用 `errors.isEmpty()` 决定是否下钻——无论父层有无错误都继续递归解析子命令（递归本身已能独立收集错误），把各层错误在 `CommandInvocation` 里合并；或至少只对会改变 token 边界的错误类型（如 `NoSuchOption`）放弃下钻。
- 相关边界（改的时候要一起考虑）：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:100`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:103` 对 `Abort` 的条件吞掉——解析期错误已经在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/ParserInternals.kt:187` 置过 `errorEncountered`，所以子命令若靠自定义 transform 抛 `Abort` 短路，会在“父层有错误”时被静默忽略。

### 风险点 3：转换错误先抛，`validate { }` 整批不执行，两类错误无法合流

- 位置：`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:157`（解析+终局化错误先抛）与 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:159`（校验在后），顺序写死在同一个函数里。
- 机理：`finalizeParameters` 收集到任何一个 `UsageError` 后，`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:157` 的 `throwErrors()` 就会抛出，`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:159` 的 `validateParameters` 根本不执行。后果有两层：(1) 用户一次只能看到“转换/缺失”这一层错误，`validate { }` 里的规则全部跳过，必须修完第一层再跑一次才暴露第二层；(2) 对“把校验错误批量回吐上游”的扩展来说，不能假设一个 `MultiUsageError` 能同时装下两类错误。更危险的是若有人把这两行“顺手合并”（把 159 移到 157 之前或合并错误列表），`postValidate` 会去读终局化失败、从未赋值的 delegate，在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parameters/options/OptionWithValues.kt:216` 抛 `LateinitException`，从批量错误退化为一个含义不明的崩溃。
- 最小复现：

  ```kotlin
  class C : CliktCommand() {
      val n by option().int()
      val m by option().validate { fail("m validator ran") }
      override fun run() = Unit
  }
  C().parse(listOf("--n", "abc", "--m", "x"))
  ```

  实际观察：只抛 `BadParameterValue`，消息为 `invalid value for --n: abc is not a valid integer`；`--m` 的 `validate { }` 完全没有执行（消息里没有 `m validator ran`，也没有包成 `MultiUsageError`）。argv 改成 `listOf("--n", "1", "--m", "x")` 时才单独抛 `BadParameterValue: m validator ran`——两类错误永远不可能同批出现。
- 最小修法：让终局化记录“哪些参数 finalize 失败”，`validateParameters` 跳过这些参数后照常执行，再把 `usageErrors + finalizationErrors + validationErrors` 合并成一次 `throwErrors()`（即把 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/parsers/CommandLineParser.kt:157` 的抛出点挪到校验之后，并在 `clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:119`-`clikt/src/commonMain/kotlin/com/github/ajalt/clikt/internal/Finalization.kt:127` 的三个循环里跳过未成功终局化的参数）。
